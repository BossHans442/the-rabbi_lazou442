#include <stdlib>
#include <stdio>
/**
 * A number that is divisible by 30 and is an odd number
 *
 *Return_ An output number less than 10
 */
{ 
	int main(void)
	if ("number is less than 20");
	return(yes);
	else
		return(none)
}
